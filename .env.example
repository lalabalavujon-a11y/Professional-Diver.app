#
# Copy to .env.local (recommended) and fill values.
# Do NOT commit .env or .env.local (already gitignored).
#

# === OpenAI (server-side) ===
OPENAI_API_KEY=your_openai_api_key_here

# Optional aliases (if your host uses different names)
# OPENAI_KEY=
# OPENAI_APIKEY=
# OPENAI_TOKEN=

# === LangSmith (optional tracing) ===
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_PROJECT=professional-diver-training-app

