# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env.local for local development
# DO NOT commit .env.local to git (it's in .gitignore)
#
# For production:
# - Server secrets → Railway (staging + prod services)
# - Client secrets → Cloudflare Pages (public vars only)
#
# ⚠️ SECURITY RULES:
# - Anything starting with VITE_ is PUBLIC (bundled into browser)
# - NEVER put SUPABASE_SERVICE_ROLE_KEY in client env (Cloudflare Pages)
# - NEVER put DATABASE_URL in client env (contains password)
# - Service role keys and DB passwords → Railway ONLY
# ============================================

# ============================================
# CLIENT-SIDE (Cloudflare Pages / Vite)
# ============================================
# These are bundled into the browser - treat as PUBLIC
# Set in: Cloudflare Pages → Settings → Environment Variables

VITE_API_URL=https://api.professionaldiver.app
VITE_WS_URL=wss://api.professionaldiver.app
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# SERVER-SIDE (Railway - Express API)
# ============================================
# These are PRIVATE - never expose to client
# Set in: Railway → Service → Variables

# Database
DATABASE_URL=postgresql://postgres:password@db.project.supabase.co:5432/postgres?sslmode=require

# Supabase (Server-side only)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# OpenAI / AI Services
OPENAI_API_KEY=sk-your_openai_api_key_here
AI_TUTOR_MODEL=gpt-4o
AI_TUTOR_TEMPERATURE=0.7
AI_TUTOR_MAX_TOKENS=2000
CONTENT_GENERATION_MODEL=gpt-4o
CONTENT_GENERATION_TEMPERATURE=0.7

# LangSmith (AI Observability)
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_PROJECT=professional-diver-training-app

# Stripe (Payment Processing)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Email Services
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# External APIs
OPENWEATHER_API_KEY=your_openweather_api_key_here
STORMGLASS_API_KEY=your_stormglass_api_key_here

# WhatsApp (if used)
WHATSAPP_APP_SECRET=your_whatsapp_app_secret_here

# Node Environment
NODE_ENV=development
